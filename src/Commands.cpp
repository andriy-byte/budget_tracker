
#include "Commands.h"


namespace commands {
    const boost::regex sign_in("[Ss]ign-in");
    const boost::regex sign_up("[Ss]ign-up");
    const boost::regex income("[Ii]ncome");
    const boost::regex costs("[Cc]osts");
    const boost::regex show("[Ss]how");
    const boost::regex report("[Rr]eport");
    const boost::regex exit("[Ee]xit");
    const boost::regex help("[Hh]elp");
    const boost::regex clear("[Cc]lear");
}